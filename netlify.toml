[build]
  command = "if [ -n \"$LANDING\" ]; then mkdir -p dist && cp responda-landing/index.html dist/; else npm run build; fi"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
